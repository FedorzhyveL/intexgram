import 'package:flutter/material.dart';

class Palette {
  //status bar
  static const statusBarColor = Colors.white;

  //app bar
  static const appBarColor = Colors.white;
  static const appBarTextColor = Colors.black;
  static const appBarIconColor = Colors.black;

  //
  static const textColor = Colors.black;
  static const borderSideColor = Colors.grey;

  //post
  static const likedPostLikeColor = Colors.red;
  static const notLikedPostLikeColor = Colors.grey;
  static const postFavoriteColor = Colors.black;

  //profile
  static const profileTabsIconColor = Colors.black;

  static const profileUnselectedTabsColor = Color.fromRGBO(214, 214, 214, 1);
  static const profileButtonDefautColor = Color.fromRGBO(238, 238, 238, 1);
  static const profileButtonSecondColor = Colors.blue;

  //or divider
  static const orDividerColor = Colors.black;

  //bottom navigation
  static const bottomNavigationIconColor = Colors.black;

  //sign button
  static const signButtonColor = Color(0xFF90CAF9);
  static const signTextColor = Colors.blue;

  //comments page
  static const disabledButtonColor = Color(0xFF90CAF9);
  static const abledButtonColor = Colors.blue;
}
